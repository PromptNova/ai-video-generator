# Forma Frontend (minimal)

This is a minimal React + Vite frontend demonstrating the comparison table and inline corrections.

Start:

```bash
cd C:/Users/pieter/Downloads/forma-frontend
npm install
npm run dev
```

Enter a comparison id in the top input to load and edit amounts.
